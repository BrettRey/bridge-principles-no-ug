# Agent Guidance

Read `CLAUDE.md` first. It contains the active project frame, infrastructure rules, and citation discipline for this paper.

Use Roughdraft checkpoints for substantial plans or user-facing prose decisions. Do not insert CriticMarkup into `main.tex`; use a companion Markdown file in `docs/plans/` when review is needed.

This project uses the central house style and central bibliography through symlinks. Do not create local copies.
